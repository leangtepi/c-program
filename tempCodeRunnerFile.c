
                if(s[studentCount].teacherid == t[j].id)
                {
                    printf("%d %s %d %d\n", s[j].id, s[j].name, s[j].age, s[j].teacherid);  
                }
            }
        }
    }
}